for sample in samples:
    #     sample_split = sample.split(",")

    #     for item in sample_split:
    #         case = item.strip().split(" ")
    #         if int(case[0]) > int(maxes[case[1]]):
    #             maxes[case[1]] = case[0]
    # power_sum += reduce(operator.mul, list(maxes.values))#int(maxes["red"]) * int(maxes["blue"]) * int(maxes["green"])
    